chapter "docker"
refer `
平台 https://hub.docker.com/
官网 https://www.docker.com/
文档 https://docs.docker.com/
源码 https://github.com/docker/docker-ce
启动 https://docs.docker.com/engine/reference/commandline/dockerd/
`

field web.code.docker.client
field web.code.docker.server

shell `
wget https://download.docker.com/linux/static/stable/x86_64/docker-19.03.9.tgz
tar xvf docker-19.03.9.tgz && cd docker/

sudo dockerd --host unix://$PWD/docker.sock --pidfile $PWD/docker.pid --exec-root=$PWD/exec --data-root=$PWD/data --registry-mirror http://hub-mirror.c.163.com
sudo chmod 0666 $PWD/docker.sock
export DOCKER_HOST=unix://$PWD/docker.sock

docker run -w /root -p 20000:9020 -it busybox
export ctx_dev=http://2021.shylinux.com:9020 ctx_pod=20230511-golang-story; temp=$(mktemp); if curl -h &>/dev/null; then curl -o $temp -fsSL $ctx_dev; else wget -O $temp -q $ctx_dev; fi; source $temp app username shylinux usernick ""

docker run -w /root -p 20001:9020 -it alpine
export ctx_dev=http://2021.shylinux.com:9020 ctx_pod=20230511-golang-story; temp=$(mktemp); if curl -h &>/dev/null; then curl -o $temp -fsSL $ctx_dev; else wget -O $temp -q $ctx_dev; fi; source $temp dev username shylinux usernick ""

docker ps
`
